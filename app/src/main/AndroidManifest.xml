<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.adri.ajm">

    <application

        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.example.adri.ajm.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.adri.ajm.RegisterActivity" />
        <activity android:name="com.example.adri.ajm.MainActivity" />
        <activity android:name="com.example.adri.ajm.NewActivity" />
        <activity android:name="com.example.adri.ajm.InfoActivity" />
        <activity android:name="com.example.adri.ajm.InfoMainActivity"></activity>

        <service android:name=".ServicioMusica" />
    </application>

</manifest>